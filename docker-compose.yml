version: '2'

services:
  app:
    build: .
    env_file:
      - .env
    ports:
      - 8000:8000
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "10"

networks:
  default:
    external:
      name: registry-migrate

# vim:ts=2:sw=2:et
